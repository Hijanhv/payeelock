{
  "name": "@payeelock/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen subgraph.yaml --output-dir generated",
    "build": "npm run codegen && graph build subgraph.yaml",
    "deploy": "graph deploy payeelock-sentinel subgraph.yaml --version-label 0.1.0"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1"
  }
}
